defmodule L2E.Duel.Registry do
  @moduledoc "Registry for Duel.Session processes, keyed by duel_id."
  # This module just provides the name for {Registry, name: L2E.Duel.Registry}
  # Started in application.ex as: {Registry, keys: :unique, name: L2E.Duel.Registry}
end
