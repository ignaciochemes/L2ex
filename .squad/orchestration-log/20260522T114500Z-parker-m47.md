# Parker — M47 Quest Infrastructure DB Layer

**Timestamp:** 2026-05-22T11:45:00Z  
**Agent:** Parker (Persistence Engineer)  
**Milestone:** M47 — Quest Infrastructure DB Layer

## Work Performed

### 1. Migration `20260522000005_create_character_quests.exs`
Created `priv/repo/migrations/20260522000005_create_character_quests.exs`. Creates `character_quests` table with:
- `character_id` (FK reference, no DB-level constraint per project convention)
- `quest_id` integer
- `state` integer default 0 (0=not started, 1=in progress, 2=complete)
- `cond` integer default 0
- `count` integer default 0
- `reward_taken` boolean default false
- Single-column index on `character_id`
- Unique composite index on `[:character_id, :quest_id]`
- Explicit `down` block drops the table.

### 2. Schema `lib/l2e/db/character_quest.ex`
Created `L2E.DB.CharacterQuest` following the `CharacterSkill` pattern. Provides:
- `load_for_character/1` — bulk load via `Ecto.Query`
- `get_quest/2` — returns `{:ok, record} | {:error, :not_found}`
- `set_quest_state/5` — upsert with `on_conflict: :replace_all`
- `complete_quest/2` — targeted upsert setting `state: 2, reward_taken: true`

Changeset validates `state` inclusion in `[0, 1, 2]` and non-negative `cond`/`count`.

### 3. Association in `lib/l2e/db/character.ex`
Added `has_many :quests, L2E.DB.CharacterQuest` after existing `has_many :skills`.

## Files Created / Modified

| File | Change |
|------|--------|
| `priv/repo/migrations/20260522000005_create_character_quests.exs` | Created |
| `lib/l2e/db/character_quest.ex` | Created |
| `lib/l2e/db/character.ex` | Added `has_many :quests` association |

## Key Decisions

- `on_conflict: :replace_all` for `set_quest_state`; `on_conflict: [set: [...]]` for `complete_quest` to avoid overwriting progress counters.
- `cond` field name kept to mirror L2 protocol terminology — Ecto handles it safely.
- No FK constraint in migration; enforced at changeset level via `foreign_key_constraint/2`.
