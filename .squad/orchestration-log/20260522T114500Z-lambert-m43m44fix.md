# Lambert — M43+M44 Duplicate Packet Fix

**Timestamp:** 2026-05-22T11:45:00Z  
**Agent:** Lambert (Network Engineer)  
**Milestone:** M43 (Private Store Buy) + M44 (Character Skills)

## Work Performed

### 1. Removed 153-line duplicate block in `client/packets.ex`
A 153-line block was duplicated in `lib/l2e/packet/client/packets.ex`. Lambert identified and removed the duplicate, restoring clean unique module definitions.

### 2. Fixed `RequestPrivateStoreSell` field name
`store_player_id` was renamed to `owner_obj_id` to match the correct protocol field naming and align with how the field is referenced in downstream handlers.

### 3. Added `RequestPrivateStoreQuitBuy` struct
New client packet `RequestPrivateStoreQuitBuy` added with opcode `0x8D`. Struct definition and decoder entry both added.

### 4. Added decoder entry for `0x8D`
`decode(0x8D, body)` entry added to `lib/l2e/packet/decoder.ex`, mapping to `RequestPrivateStoreQuitBuy`.

## Files Modified

| File | Change |
|------|--------|
| `lib/l2e/packet/client/packets.ex` | Removed 153-line duplicate block |
| `lib/l2e/packet/client/packets.ex` | Fixed `store_player_id` → `owner_obj_id` in `RequestPrivateStoreSell` |
| `lib/l2e/packet/client/packets.ex` | Added `RequestPrivateStoreQuitBuy` struct |
| `lib/l2e/packet/decoder.ex` | Added `decode(0x8D, ...)` entry |

## Outcome

Clean compile. M43 private store buy-side packet pipeline corrected.
